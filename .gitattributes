# Development on Windows, execution on Linux: shell scripts and templates
# MUST have LF, otherwise the shebang fails ("bad interpreter: \r").
* text=auto eol=lf
*.sh text eol=lf
*.tmpl text eol=lf
*.md text eol=lf
